# 第0步：项目基础架构任务书

## 0.1 目标

建立一个可运行的前后端分离项目框架，能够连接 SQL Server，能够启动后端和前端，但暂不开发具体业务。

## 0.2 后端任务

1. 创建 FastAPI 项目。
2. 配置 CORS。
3. 配置 SQL Server 数据库连接。
4. 配置日志。
5. 配置统一异常处理。
6. 配置统一返回格式。
7. 配置健康检查接口。
8. 配置多数据库连接框架。
9. 编写 README 启动说明。

## 0.3 后端接口

### GET `/api/health`

返回：

```json
{
  "success": true,
  "message": "服务正常",
  "data": {
    "app": "CZZY_WEB",
    "database": "connected"
  },
  "code": 200
}
```

### GET `/api/db/check`

检查各数据库连接：

```json
{
  "order_db": true,
  "code_db": true,
  "mold_db": true,
  "key_db": true
}
```

## 0.4 前端任务

1. 创建 Vue3 + Vite 项目。
2. 安装 Element Plus。
3. 配置 Axios。
4. 配置基础路由。
5. 创建空白首页。
6. 创建基础布局组件。
7. 配置手机端基础响应式样式。

## 0.5 完成标准

1. 后端可通过 `uvicorn app.main:app --host 0.0.0.0 --port 8000` 启动。
2. 前端可通过 `npm run dev` 启动。
3. 前端能访问后端健康检查接口。
4. 局域网其他电脑能访问服务器 IP。
5. 手机浏览器能打开首页。

---


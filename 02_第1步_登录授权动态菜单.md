# 第1步：登录、授权、动态菜单任务书

## 1.1 目标

实现系统登录、LEEToolsKey 验证、动态菜单和基础权限控制。

## 1.2 原系统逻辑

```text
系统启动
→ 登录页 frmlogin
→ 用户名密码校验
→ LEEToolsKey 验证
→ 验证通过后读取显示项目 KEYwindow
→ 进入主界面 frmFrom
→ 按白名单显示菜单
```

## 1.3 后端任务

1. 实现登录接口。
2. 实现 JWT Token。
3. 实现当前用户接口。
4. 实现退出接口。
5. 实现 LEEToolsKey 校验服务。
6. 实现菜单接口。
7. 预留 RBAC 表结构或配置结构：
   - 用户
   - 角色
   - 菜单
   - 按钮权限
   - 用户角色关系
   - 角色菜单关系

## 1.4 接口设计

### POST `/api/auth/login`

请求：

```json
{
  "username": "admin",
  "password": "123456"
}
```

返回：

```json
{
  "access_token": "xxx",
  "token_type": "bearer",
  "user": {
    "id": 1,
    "username": "admin",
    "display_name": "管理员"
  }
}
```

### GET `/api/auth/me`

返回当前登录用户信息。

### GET `/api/menus/my`

返回当前用户菜单。

菜单结构示例：

```json
[
  {
    "name": "销售管理",
    "path": "/sales",
    "children": [
      {"name": "新建客户", "path": "/sales/customers/new"},
      {"name": "查询编辑客户", "path": "/sales/customers"}
    ]
  }
]
```

### POST `/api/auth/logout`

退出登录。

## 1.5 前端页面

1. `/login` 登录页
2. `/` 主框架
3. 动态菜单
4. 手机端折叠菜单
5. 顶部用户信息
6. 首页 Dashboard

## 1.6 菜单范围

一级菜单必须包含：

1. 销售管理
2. 订单管理
3. 编码管理
4. 挤出生产管理
5. 切割制品管理
6. 检验管理
7. 仓库管理
8. 技术
9. 数据查询
10. 计算
11. 管理员设置
12. 库存编码
13. 关于
14. 退出

## 1.7 完成标准

1. 能登录。
2. 未登录访问业务页自动跳转登录。
3. 登录后显示菜单。
4. 手机端菜单可展开、收起。
5. 后端接口有权限校验依赖。

---


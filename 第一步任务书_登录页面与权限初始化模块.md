# 第一步任务书：登录页面与权限初始化模块

## 1. 任务背景

本项目为 CZZY 公司内部管理系统，当前 GitHub 仓库为：

```text
https://github.com/shushu8589/CZZYDEEPSEEK
```

目前项目已新增数据库表的详细结构。第一步开发内容需要根据新增数据库结构进行调整，优先完成系统登录页面、登录验证、用户信息读取、权限字段读取及主界面框架初始化。

第一步不开发具体业务模块，重点是为后续报价、合同、批单、仓库、生产、检验、基础数据等模块提供统一的登录入口和权限基础。

---

## 2. 本阶段开发目标

本阶段目标是完成系统的统一登录入口。

具体目标如下：

1. 用户打开程序后，首先进入登录页面。
2. 用户输入账号和密码后，程序连接 SQL Server 数据库进行验证。
3. 登录数据来源于 `CZZYKeyList.dbo.LEEToolsKey` 表。
4. 登录成功后，读取当前用户的姓名、账号、可显示项目、最后登录时间、登录 IP 等信息。
5. 登录成功后，更新用户最后登录时间、登录 IP、登录历史。
6. 登录成功后进入主界面框架。
7. 主界面显示当前登录人姓名。
8. 主界面根据 `显示项目` 字段预留菜单显示控制逻辑。
9. 登录失败或数据库连接失败时，给出清晰提示。
10. 全部中文必须正常显示，不能出现乱码。

---

## 3. 本阶段使用数据库

### 3.1 登录数据库

本阶段主要使用数据库：

```text
CZZYKeyList
```

### 3.2 登录数据表

本阶段主要使用数据表：

```text
dbo.LEEToolsKey
```

### 3.3 登录表字段

| 字段名 | 数据类型 | 是否为空 | 说明 |
|---|---|---|---|
| `KEY_id` | INT | NOT NULL | 用户记录主键，自动编号 |
| `KEY_name` | NVARCHAR(10) | NOT NULL | 登录账号 |
| `KEY_LastTime` | DATETIME | NULL | 最后一次登录时间 |
| `KEY_password` | NVARCHAR(50) | NOT NULL | 登录密码 |
| `显示项目` | NVARCHAR(max) | NULL | 用户可显示项目或功能权限 |
| `Person` | NVARCHAR(50) | NOT NULL | 用户姓名 |
| `Login_History` | NVARCHAR(max) | NULL | 登录历史记录 |
| `KEY_IP` | NVARCHAR(50) | NULL | 最近登录 IP |

---

## 4. 第一阶段功能范围

### 4.1 本阶段需要完成的内容

本阶段需要完成以下内容：

1. 登录页面。
2. 数据库连接配置。
3. 登录账号和密码验证。
4. 当前用户信息读取。
5. 登录成功后的用户信息保存。
6. 登录成功后的登录信息更新。
7. 主界面框架。
8. 根据 `显示项目` 字段预留菜单控制。
9. 登录异常提示。
10. 中文编码处理。

### 4.2 本阶段暂不开发的内容

为了保证第一步稳定、清晰、可测试，本阶段暂不开发以下内容：

1. 不开发报价管理具体页面。
2. 不开发合同管理具体页面。
3. 不开发批单管理具体页面。
4. 不开发仓库管理具体页面。
5. 不开发生产管理具体页面。
6. 不开发检验管理具体页面。
7. 不开发基础数据维护页面。
8. 不开发用户新增、删除、修改功能。
9. 不开发密码修改功能。
10. 不开发复杂角色权限管理。
11. 不开发业务报表和统计功能。
12. 不对现有业务数据库进行增删改操作。

---

## 5. 登录页面设计要求

### 5.1 页面名称

建议页面名称：

```text
登录页面
```

或：

```text
LoginPage
```

### 5.2 页面显示内容

登录页面应包含以下内容：

1. 系统名称。
2. 账号输入框。
3. 密码输入框。
4. 登录按钮。
5. 退出按钮。
6. 状态提示区域。

### 5.3 系统名称建议

页面顶部建议显示：

```text
CZZY 公司管理系统
```

或：

```text
CZZY 企业内部管理平台
```

### 5.4 账号输入框

账号输入框对应数据库字段：

```text
KEY_name
```

要求：

1. 账号不能为空。
2. 自动去除账号前后空格。
3. 支持中文或英文账号显示，但建议账号本身按数据库现有规则处理。
4. 输入框应有提示文字，例如：

```text
请输入账号
```

### 5.5 密码输入框

密码输入框对应数据库字段：

```text
KEY_password
```

要求：

1. 密码不能为空。
2. 密码输入时应以隐藏字符显示。
3. 输入框应有提示文字，例如：

```text
请输入密码
```

### 5.6 登录按钮

点击登录按钮后执行登录验证流程。

按钮文字建议：

```text
登录
```

### 5.7 退出按钮

点击退出按钮后关闭程序或返回上一页。

按钮文字建议：

```text
退出
```

### 5.8 状态提示区域

状态提示区域用于显示登录过程中的提示信息。

常见提示包括：

```text
正在连接数据库……
请输入账号
请输入密码
账号或密码错误
数据库连接失败，请检查服务器或网络
登录成功，正在进入系统……
登录成功，但未配置功能权限
用户信息读取失败，请联系管理员
```

---

## 6. 中文乱码处理要求

本系统涉及大量中文字段和中文页面内容，第一步必须处理好中文乱码问题。

### 6.1 Python 文件编码

如果项目使用 Python，所有 `.py` 文件建议统一使用 UTF-8 编码，并在文件开头加入：

```python
# -*- coding: utf-8 -*-
```

### 6.2 HTML 文件编码

如果项目使用 HTML 页面，必须在 `<head>` 中加入：

```html
<meta charset="UTF-8">
```

### 6.3 JSON 返回中文

如果后端接口返回 JSON，应保证中文正常显示，不要被错误转码。

例如 Python 中可注意：

```python
json.dumps(data, ensure_ascii=False)
```

或使用框架默认支持 UTF-8 的响应方式。

### 6.4 数据库字段

数据库中中文字段已经使用 `NVARCHAR`，程序读取时应按 Unicode 字符串处理。

涉及中文字段包括：

```text
Person
显示项目
Login_History
```

### 6.5 页面字体

页面应使用支持中文的字体，例如：

```text
Microsoft YaHei
微软雅黑
SimSun
宋体
```

---

## 7. 数据库连接要求

### 7.1 配置文件管理

数据库连接信息不要直接写死在业务代码中，应放入配置文件。

建议配置文件名称：

```text
config.json
```

或根据现有项目结构使用已有配置文件。

### 7.2 配置文件建议内容

```json
{
  "database": {
    "server": "服务器地址",
    "user": "数据库账号",
    "password": "数据库密码",
    "database": "CZZYKeyList",
    "driver": "ODBC Driver 17 for SQL Server"
  }
}
```

如果现有项目已经有配置文件，应尽量沿用现有结构，不要重复创建多个配置入口。

### 7.3 第一阶段连接目标

第一阶段只要求连接登录数据库：

```text
CZZYKeyList
```

后续业务模块再逐步连接：

```text
CZZYOrder
CZZYCODE
CZZYMOLDCODE
```

### 7.4 数据库连接异常处理

如果数据库连接失败，应在登录页面提示：

```text
数据库连接失败，请检查服务器或网络
```

同时程序内部应记录详细错误信息，便于后续排查。

---

## 8. 登录验证逻辑

### 8.1 登录流程

用户点击登录按钮后，程序按以下流程执行：

1. 获取账号输入值。
2. 获取密码输入值。
3. 判断账号是否为空。
4. 判断密码是否为空。
5. 连接 `CZZYKeyList` 数据库。
6. 查询 `LEEToolsKey` 表。
7. 根据 `KEY_name` 查找用户。
8. 如果账号不存在，提示账号或密码错误。
9. 如果账号存在，比较 `KEY_password`。
10. 如果密码错误，提示账号或密码错误。
11. 如果密码正确，读取用户信息。
12. 更新登录时间、登录 IP、登录历史。
13. 保存当前用户信息。
14. 进入主界面框架。

---

## 9. 登录查询 SQL 建议

### 9.1 查询用户

建议使用参数化查询，避免 SQL 注入。

```sql
SELECT 
    KEY_id,
    KEY_name,
    KEY_password,
    Person,
    显示项目,
    KEY_LastTime,
    Login_History,
    KEY_IP
FROM CZZYKeyList.dbo.LEEToolsKey
WHERE KEY_name = @KEY_name
```

如果使用 Python，注意根据具体数据库驱动调整参数占位符。

例如 pyodbc 常见写法为：

```sql
SELECT 
    KEY_id,
    KEY_name,
    KEY_password,
    Person,
    显示项目,
    KEY_LastTime,
    Login_History,
    KEY_IP
FROM CZZYKeyList.dbo.LEEToolsKey
WHERE KEY_name = ?
```

---

## 10. 密码验证要求

### 10.1 当前阶段

根据现有数据库结构，`KEY_password` 字段类型为：

```text
NVARCHAR(50)
```

第一阶段可以先按现有数据进行密码比对。

即：

```text
用户输入密码 == 数据库 KEY_password
```

这样便于先把系统登录流程跑通。

### 10.2 后续优化建议

后续版本建议逐步改为：

1. 密码加密存储。
2. 密码加盐哈希。
3. 登录失败次数限制。
4. 密码修改功能。
5. 管理员重置密码功能。
6. 用户禁用功能。

以上内容不作为第一步必须完成内容。

---

## 11. 登录成功后读取的用户信息

登录成功后，需要保存当前用户信息。

建议形成统一的当前用户对象，例如：

```text
CurrentUser
```

### 11.1 当前用户信息内容

| 当前用户属性 | 来源字段 | 说明 |
|---|---|---|
| `user_id` | `KEY_id` | 用户 ID |
| `username` | `KEY_name` | 登录账号 |
| `person` | `Person` | 用户姓名 |
| `visible_projects` | `显示项目` | 可显示项目或权限字段 |
| `last_login_time` | `KEY_LastTime` | 上次登录时间 |
| `last_ip` | `KEY_IP` | 上次登录 IP |

### 11.2 当前用户信息用途

当前用户信息用于：

1. 主界面显示当前登录人。
2. 控制左侧菜单显示。
3. 后续业务模块记录操作人。
4. 后续业务模块进行权限判断。
5. 后续日志记录。

---

## 12. 登录成功后更新数据库

登录成功后，应更新以下字段：

1. `KEY_LastTime`
2. `KEY_IP`
3. `Login_History`

### 12.1 更新 SQL 建议

```sql
UPDATE CZZYKeyList.dbo.LEEToolsKey
SET 
    KEY_LastTime = GETDATE(),
    KEY_IP = @CurrentIP,
    Login_History = ISNULL(Login_History, '') + @NewLoginHistory
WHERE KEY_id = @KEY_id
```

如果使用 pyodbc，可调整为：

```sql
UPDATE CZZYKeyList.dbo.LEEToolsKey
SET 
    KEY_LastTime = GETDATE(),
    KEY_IP = ?,
    Login_History = ISNULL(Login_History, '') + ?
WHERE KEY_id = ?
```

### 12.2 登录历史格式建议

新增登录历史建议格式为：

```text
2026-05-07 10:30:25 | IP: 192.168.1.100 | 登录成功
```

每条记录之间建议换行。

例如：

```text
2026-05-07 10:30:25 | IP: 192.168.1.100 | 登录成功
2026-05-08 08:15:42 | IP: 192.168.1.101 | 登录成功
```

### 12.3 登录历史字段说明

当前数据库已有 `Login_History` 字段，因此第一阶段可以直接使用该字段。

后续如果登录历史越来越多，建议单独建立登录日志表，例如：

```text
LoginLog
```

但第一阶段不新增表。

---

## 13. IP 地址获取要求

登录成功后，需要记录当前用户 IP。

### 13.1 桌面程序

如果是桌面程序，可以记录本机局域网 IP。

### 13.2 Web 程序

如果是 Web 程序，可以从请求中获取客户端 IP。

需要注意：

1. 如果经过代理，可能需要处理 `X-Forwarded-For`。
2. 第一阶段可以先记录直接获取到的 IP。
3. 如果无法获取 IP，可记录为空或记录 `unknown`。

---

## 14. 主界面框架要求

登录成功后进入主界面框架。

### 14.1 主界面基础内容

主界面至少包含：

1. 顶部标题栏。
2. 当前登录人显示。
3. 左侧功能菜单区域。
4. 中间内容显示区域。
5. 底部状态栏，选做。

### 14.2 当前登录人显示

主界面应显示：

```text
当前用户：张三
```

对应字段：

```text
Person
```

如果 `Person` 为空，应显示账号：

```text
当前用户：KEY_name
```

### 14.3 功能菜单预留

主界面左侧菜单可先预留以下功能：

```text
报价管理
合同管理
批单管理
仓库管理
生产管理
检验管理
基础数据
系统设置
```

第一阶段点击这些菜单时，可以暂时提示：

```text
该功能将在后续步骤开发
```

---

## 15. 显示项目字段的权限控制

### 15.1 字段名称

权限控制字段为：

```text
显示项目
```

来源表：

```text
CZZYKeyList.dbo.LEEToolsKey
```

### 15.2 字段用途

该字段用于控制用户可看到哪些功能模块。

第一阶段只要求完成：

```text
读取字段 → 解析字段 → 控制菜单显示
```

不要求实现复杂权限系统。

### 15.3 推荐格式

如果数据库中已有固定格式，应优先兼容原格式。

如果暂未固定，建议采用分号分隔。

中文格式示例：

```text
报价管理;合同管理;批单管理;仓库管理;生产管理;检验管理;基础数据
```

英文格式示例：

```text
quote;contract;order;warehouse;production;inspection;base_data
```

### 15.4 菜单控制示例

| `显示项目` 包含内容 | 主界面显示菜单 |
|---|---|
| `报价管理` | 显示报价管理 |
| `合同管理` | 显示合同管理 |
| `批单管理` | 显示批单管理 |
| `仓库管理` | 显示仓库管理 |
| `生产管理` | 显示生产管理 |
| `检验管理` | 显示检验管理 |
| `基础数据` | 显示基础数据 |

### 15.5 权限为空处理

如果用户登录成功，但 `显示项目` 为空，可以有两种处理方式。

推荐第一阶段采用方式一。

#### 方式一：允许进入主界面，但不显示业务菜单

提示：

```text
登录成功，但未配置功能权限，请联系管理员
```

#### 方式二：不允许进入主界面

提示：

```text
当前账号未配置功能权限，请联系管理员
```

---

## 16. 异常提示要求

登录页面至少应处理以下异常：

| 异常情况 | 页面提示 |
|---|---|
| 账号为空 | 请输入账号 |
| 密码为空 | 请输入密码 |
| 数据库连接失败 | 数据库连接失败，请检查服务器或网络 |
| 账号不存在 | 账号或密码错误 |
| 密码错误 | 账号或密码错误 |
| 用户信息读取失败 | 用户信息读取失败，请联系管理员 |
| 权限字段为空 | 登录成功，但未配置功能权限 |
| 登录信息更新失败 | 登录成功，但登录记录更新失败 |
| 未知异常 | 系统异常，请联系管理员 |

---

## 17. 日志记录要求

第一阶段建议至少在程序内部记录以下日志：

1. 程序启动时间。
2. 数据库连接成功或失败。
3. 登录成功用户。
4. 登录失败账号。
5. 登录异常信息。
6. 主界面加载情况。

日志可以先输出到控制台，也可以写入本地日志文件。

如果现有项目已有日志模块，应沿用现有日志模块。

---

## 18. 文件结构建议

具体文件结构应尽量结合现有项目，不要大幅重构。

如果当前项目尚未形成固定结构，可参考以下结构：

```text
CZZYDEEPSEEK/
│
├─ main.py
├─ config.json
│
├─ backend/
│  ├─ database.py
│  ├─ auth_service.py
│  └─ user_context.py
│
├─ frontend/
│  ├─ login.html
│  ├─ main.html
│  ├─ css/
│  └─ js/
│
└─ logs/
```

如果是桌面程序，可参考：

```text
CZZYDEEPSEEK/
│
├─ main.py
├─ config.json
│
├─ services/
│  ├─ database.py
│  ├─ auth_service.py
│  └─ user_context.py
│
├─ ui/
│  ├─ login_window.py
│  └─ main_window.py
│
└─ logs/
```

注意：以上只是建议，实际开发应优先保持现有项目结构。

---

## 19. 后端接口建议

如果项目采用前后端分离或 Web 方式，建议预留以下接口。

### 19.1 登录接口

```text
POST /api/login
```

请求示例：

```json
{
  "username": "admin",
  "password": "123456"
}
```

返回成功示例：

```json
{
  "success": true,
  "message": "登录成功",
  "user": {
    "user_id": 1,
    "username": "admin",
    "person": "管理员",
    "visible_projects": "报价管理;合同管理;仓库管理"
  }
}
```

返回失败示例：

```json
{
  "success": false,
  "message": "账号或密码错误"
}
```

### 19.2 当前用户接口，选做

```text
GET /api/current-user
```

用于后续页面刷新后获取当前登录用户。

第一阶段如果不是 Web 架构，可以不做该接口。

---

## 20. 登录页面测试要求

第一阶段完成后，需要至少完成以下测试。

### 20.1 正常登录测试

测试内容：

1. 输入正确账号。
2. 输入正确密码。
3. 点击登录。
4. 成功进入主界面。
5. 主界面显示当前用户姓名。
6. 主界面按 `显示项目` 显示菜单。
7. 数据库 `KEY_LastTime` 更新。
8. 数据库 `KEY_IP` 更新。
9. 数据库 `Login_History` 追加记录。

### 20.2 错误账号测试

测试内容：

1. 输入不存在账号。
2. 输入任意密码。
3. 点击登录。
4. 页面提示：

```text
账号或密码错误
```

### 20.3 错误密码测试

测试内容：

1. 输入正确账号。
2. 输入错误密码。
3. 点击登录。
4. 页面提示：

```text
账号或密码错误
```

### 20.4 空账号测试

测试内容：

1. 账号为空。
2. 输入密码。
3. 点击登录。
4. 页面提示：

```text
请输入账号
```

### 20.5 空密码测试

测试内容：

1. 输入账号。
2. 密码为空。
3. 点击登录。
4. 页面提示：

```text
请输入密码
```

### 20.6 数据库连接失败测试

测试内容：

1. 临时设置错误数据库服务器地址。
2. 点击登录。
3. 页面提示：

```text
数据库连接失败，请检查服务器或网络
```

### 20.7 中文显示测试

测试内容：

1. 登录页面中文正常。
2. 主界面中文正常。
3. `Person` 中文姓名正常。
4. `显示项目` 中文菜单正常。
5. 登录历史中文正常。
6. 不出现乱码。

---

## 21. 验收标准

第一步完成后，满足以下条件视为通过验收：

1. 程序启动后能显示登录页面。
2. 登录页面中文无乱码。
3. 可以输入账号和密码。
4. 可以连接 `CZZYKeyList` 数据库。
5. 可以查询 `dbo.LEEToolsKey` 表。
6. 可以按 `KEY_name` 查询用户。
7. 可以按 `KEY_password` 验证密码。
8. 登录成功后可以进入主界面。
9. 主界面可以显示当前用户姓名。
10. 可以读取 `显示项目` 字段。
11. 主界面可以根据 `显示项目` 字段预留菜单控制。
12. 登录成功后可以更新 `KEY_LastTime`。
13. 登录成功后可以更新 `KEY_IP`。
14. 登录成功后可以追加 `Login_History`。
15. 登录失败时提示清楚。
16. 数据库连接失败时提示清楚。
17. 代码结构清晰，便于后续继续开发。
18. 不破坏现有项目结构。
19. 不影响后续业务模块扩展。

---

## 22. 后续步骤预留

第一步完成后，后续可以按以下顺序继续开发：

1. 第二步：主界面菜单与页面框架完善。
2. 第三步：基础数据模块。
3. 第四步：客户资料模块。
4. 第五步：报价管理模块。
5. 第六步：合同管理模块。
6. 第七步：批单管理模块。
7. 第八步：生产任务模块。
8. 第九步：检验入库模块。
9. 第十步：仓库管理模块。
10. 第十一步：统计查询与报表模块。
11. 第十二步：权限管理和系统设置模块。

---

## 23. 给 Codex 的直接开发指令

请将以下内容直接发给 Codex：

```markdown
请根据当前 GitHub 项目代码，完成第一步：登录页面与权限初始化模块。

项目仓库：
https://github.com/shushu8589/CZZYDEEPSEEK

数据库使用 SQL Server。

第一步主要使用数据库：
CZZYKeyList

登录表：
dbo.LEEToolsKey

字段如下：
- KEY_id：用户ID，主键，自动编号
- KEY_name：登录账号
- KEY_LastTime：最后登录时间
- KEY_password：登录密码
- 显示项目：用户可见功能项目或权限配置
- Person：用户姓名
- Login_History：登录历史
- KEY_IP：最近登录IP

功能要求：
1. 制作登录页面，包含账号、密码、登录按钮、退出按钮和状态提示。
2. 登录时连接 CZZYKeyList 数据库。
3. 按 KEY_name 查询 dbo.LEEToolsKey 表。
4. 校验 KEY_password。
5. 登录成功后读取 KEY_id、KEY_name、Person、显示项目、KEY_LastTime、KEY_IP。
6. 登录成功后保存当前用户信息，供主界面使用。
7. 登录成功后更新 KEY_LastTime、KEY_IP、Login_History。
8. 登录成功后进入主界面框架。
9. 主界面显示当前登录人姓名。
10. 主界面根据“显示项目”字段预留菜单显示控制逻辑。
11. 登录失败时提示“账号或密码错误”。
12. 数据库连接失败时提示“数据库连接失败，请检查服务器或网络”。
13. 全部中文必须正常显示，不能乱码。
14. 数据库连接信息不要写死在业务代码中，应放到配置文件中。
15. 第一阶段只实现登录、用户信息读取、权限字段读取、主界面框架，不开发具体业务模块。
16. 菜单可以先预留：报价管理、合同管理、批单管理、仓库管理、生产管理、检验管理、基础数据、系统设置。
17. 点击尚未开发的菜单时，可以提示“该功能将在后续步骤开发”。
18. 不要破坏现有项目结构，尽量在现有代码基础上增量修改。
19. 请完成必要注释，方便后续继续开发订单、报价、合同、仓库、生产、检验等模块。
20. 请特别注意中文编码问题，所有页面和接口统一使用 UTF-8。
```

---

## 24. 特别注意事项

### 24.1 不要一次性开发过多内容

第一步只做登录和主框架，不要直接开发完整 ERP 功能。

### 24.2 不要破坏现有数据库

第一步只读取登录表，并更新登录相关字段，不要修改其他业务表。

### 24.3 不要改变现有表结构

除非后续明确要求，否则第一步不要新增、删除或修改数据库字段。

### 24.4 优先保证可运行

第一步最重要的是：

```text
能登录
能进入主界面
中文不乱码
后续好扩展
```

---

## 25. 本阶段最终交付内容

Codex 完成后，应提交以下内容：

1. 登录页面代码。
2. 数据库连接配置代码。
3. 登录验证逻辑。
4. 当前用户信息保存逻辑。
5. 登录历史更新逻辑。
6. 主界面框架。
7. 菜单权限预留逻辑。
8. 必要的说明和注释。
9. 测试说明。

第一步完成后，用户应可以基于真实数据库账号进行登录测试。
